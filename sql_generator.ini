[sql]
prefix_template =
    SELECT F0UCUS1, F0UIDPL, F0UEAN, F0USCRD, F0USCON, SCACT
    FROM {schema1}.F0UPF
    INNER JOIN {schema2}.NEPF on NEEAN=F0UEAN
    INNER join {schema2}.SCPF on NEAN=SCAN AND NEAB=SCAB AND NEAS=SCAS
    WHERE 1=1
    AND {filter} IN (